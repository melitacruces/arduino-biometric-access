# Credenciales maestras del administrador.
# Copia este archivo como ".env" (en la misma carpeta src/app/) y reemplaza los valores.
# El archivo .env real está ignorado por git y NO debe subirse al repositorio.

ADMIN_USER=admin
ADMIN_PIN=12345
